<UserControl x:Class="MigrationStudio.Desktop.Views.PluginsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="28" FontWeight="SemiBold" Text="{Binding Title}" />
        <TextBlock Grid.Row="1" Margin="0,10,0,16"
                   Foreground="{DynamicResource SecondaryTextBrush}" Text="{Binding Summary}" />
        <DataGrid Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Plugins}">
            <DataGrid.Columns>
                <DataGridTextColumn Width="2*" Binding="{Binding Metadata.DisplayName}" Header="Plugin" />
                <DataGridTextColumn Width="*" Binding="{Binding Metadata.Version}" Header="Version" />
                <DataGridTextColumn Width="*" Binding="{Binding State}" Header="State" />
                <DataGridTextColumn Width="3*" Binding="{Binding Diagnostic}" Header="Diagnostic" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
